import React from "react";
import { Typography } from "@mui/material";

const ClientDashboard: React.FC = () => {
  return <Typography variant="h4">Client Dashboard</Typography>;
};

export default ClientDashboard;
